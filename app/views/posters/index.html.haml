%h1	Ogłoszenia

%table
	%th "Publishing Date"
	%th "Description"
	%th 
	
	- @posters.each do |poster|
		%tr
			%td= link_to poster.title, poster_path(poster)
			%td= poster.description
			%td= link_to "Edit", edit_poster_path(poster)
			%td= link_to "Delete", poster_path(poster), method: :delete