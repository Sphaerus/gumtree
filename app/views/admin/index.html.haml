%h1= title "Provinces"
%p= link_to "Add new province", new_admin_country_province_path(@country)

%table.table.table-striped
	%th= "Name"
	%th
	- @provinces.each do |provinces|
		%tr
			%td= province.name
			%td